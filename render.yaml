services:
  - type: web
    name: nestinsight
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: GROQ_MODEL
        value: openai/gpt-oss-20b